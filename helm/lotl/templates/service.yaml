apiVersion: v1
kind: Service
metadata:
  name: {{ include "lotl.fullname" . }}
  labels:
    {{- include "lotl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lotl.selectorLabels" . | nindent 4 }}
